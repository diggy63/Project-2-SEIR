<%- include('../partials/header') %>
    <div class="container-fluid">
    <% drinks.forEach(function(d, i){ %>
        <% if ((i+1)%3 === 1) { %>
            <div class="row">
        <% } %>
     <div class="col">
        <div class="d-flex align-items-center flex-column">
            <div class="wrapper">
                <div id="innerContent">
                <div class="d-flex align-items-center flex-column">
                    <div class="drinkTitle"><%= d.name %></div>
         <img class="frontPageImg" src="/uploads/<%=d.imageName%>">

        <a href="/drinks/<%= d.id%>"><button class="btn">VIEW</button></a>
        </div>
    </div>
    </div>  
        </div>
    </div>
    <% if ((i+1)%3 === 0) { %>
            </div>
    <% } %>
    <% }) %>
</div> 
    

 <%- include('../partials/footer') %>